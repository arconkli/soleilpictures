node_modules/
dist/
# Vestigial pre-consolidation migrations leftover (root-owned on disk; the
# canonical migrations live at the repo-root supabase/ dir). Untracked 2026-06-10.
supabase/
test-results/
playwright-report/
.env
.env.local
.env.*.local
.DS_Store
*.log

# Generated marketing assets (npm run shots). Deliverables you upload, not app
# assets the site serves — regenerate them rather than committing them. Leading
# slash: anchored to boards/, so it does NOT swallow scripts/shots/, which holds
# the committed shot lists that produce them.
/shots/
